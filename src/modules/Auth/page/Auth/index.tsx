import {AuthBlock} from '../../components/AuthBlock';
import {AuthPageContainer} from './styled';

export const AuthPage = () => {
    return (
        <AuthPageContainer>
            <AuthBlock />
        </AuthPageContainer>
    );
};

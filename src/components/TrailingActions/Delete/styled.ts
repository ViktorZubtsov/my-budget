import {colors} from '@salutejs/plasma-tokens';
import styled from 'styled-components';

export const DeleteStyled = styled.div`
    align-items: center;
    background: ${colors.buttonCritical};
    cursor: pointer;
    display: flex;
    justify-content: center;
    padding: 16px;
`;
